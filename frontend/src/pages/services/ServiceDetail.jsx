import { Navigate, useParams } from "react-router-dom";
import Navbar from "../../components/Navbar"; import Footer from "../../components/Footer"; import SEO from "../../components/SEO";
import aiStrategyHero from "../../assets/ai-strategy-hero.png";
import generativeAiHero from "../../assets/services-hero-v2.png";
import aiAgentsHero from "../../assets/case_studies_rag_assistant.jpg";
import dataAnalyticsHero from "../../assets/hero_2.jpg";
import webDevelopmentHero from "../../assets/services_1.jpg";
import cloudDevopsHero from "../../assets/hero_1.jpg";
import { allServices, getService, legacyServiceRoutes } from "../../data/services"; import { ArchitectureDiagram, Breadcrumbs, CheckList, CTA, FAQ, SectionHeader, ServiceCard } from "../../components/services/ServiceUI";

const serviceHeroImages = {
  "ai-strategy-consulting": aiStrategyHero,
  "generative-ai-development": generativeAiHero,
  "ai-agents": aiAgentsHero,
  "data-warehousing-analytics": dataAnalyticsHero,
  "web-application-development": webDevelopmentHero,
  "devops-ci-cd": cloudDevopsHero,
};

const applyServiceHeroImage = () => {
  const image = Object.entries(serviceHeroImages).find(([slug]) => window.location.pathname.endsWith(`/${slug}`))?.[1];
  if (!image) return;
  const hero = document.querySelector("main > section");
  if (!hero || hero.dataset.serviceHeroImage) return;
  hero.dataset.serviceHeroImage = "true";
  hero.style.backgroundImage = `linear-gradient(90deg, rgba(7, 27, 59, 0.96) 0%, rgba(7, 27, 59, 0.79) 50%, rgba(7, 27, 59, 0.34) 100%), url(${image})`;
  hero.style.backgroundSize = "cover";
  hero.style.backgroundPosition = "center";
};

if (typeof window !== "undefined") {
  new MutationObserver(applyServiceHeroImage).observe(document.documentElement, { childList: true, subtree: true });
}
const genericFaqs = (service) => [[`What does a ${service.name.toLowerCase()} engagement include?`, "The scope is defined during discovery and usually covers the problem, solution design, delivery plan, implementation, validation and a clear handover or improvement path."],["Can this work with our current systems?", "We assess integration points, data flows, identity controls and operational constraints before recommending an approach."],["How do you manage sensitive data?", "We design access controls, secure APIs, input validation, auditability and human review around the sensitivity of the information involved."],["Can we start with a proof of concept?", "Yes. A focused proof of concept is often the right way to test feasibility, user value and operating assumptions before expansion."],["What happens after launch?", "We can support measurement, issue investigation, monitoring and evidence-led improvements based on an agreed engagement model."],["How does a project begin?", "Start with a conversation about the problem, users, systems and desired result. We then propose an appropriate discovery or delivery step."]];
export default function ServiceDetail() { const { categorySlug, serviceSlug, slug } = useParams(); if (slug && legacyServiceRoutes[slug]) { const [category, service] = legacyServiceRoutes[slug]; return <Navigate to={`/services/${category}/${service}`} replace/>; } const service = getService(categorySlug, serviceSlug); if (!service) return <Navigate to="/services" replace/>; const related = allServices.filter((item) => item.categorySlug === categorySlug && item.slug !== service.slug).slice(0, 3); const challenges = ["A manual or inconsistent process is slowing teams down.", "Important information is distributed across systems or documents.", "Existing tools do not provide the control, visibility or experience users need.", "A promising initiative needs a clear path from idea to dependable operation."]; const useCases = [["Operations team", `A team needs to complete repetitive work with fewer handoffs. We design ${service.name.toLowerCase()} around the current workflow and clear approval points.`, "A more traceable process with less avoidable effort."],["Business leaders", "Decision-makers need a reliable view of the work, risks and outcomes involved.", "Clearer information for prioritization and operational oversight."],["Customers or employees", "People need a simpler way to access a service, complete a task or find trusted information.", "A more usable, consistent digital experience."]]; return <><SEO title={`${service.name} | ARINSA AI MINDS`} description={service.shortDescription} canonical={`/services/${categorySlug}/${serviceSlug}`}/><Navbar/><main className="text-[#071B3B]"><section className="bg-gradient-to-br from-[#071B3B] via-[#0a2d68] to-[#1559bd] text-white py-20 lg:py-28"><div className="max-w-[1320px] mx-auto px-6"><Breadcrumbs items={[{label:"Home",to:"/"},{label:"Services",to:"/services"},{label:service.categoryName,to:`/services/${categorySlug}`},{label:service.name}]}/><p className="uppercase tracking-[.16em] text-blue-200 font-bold text-sm mt-10">{service.categoryName}</p><h1 className="text-5xl md:text-6xl lg:text-7xl font-bold leading-tight mt-4 max-w-4xl">{service.name}</h1><p className="text-xl leading-8 text-blue-50/90 mt-7 max-w-3xl">{service.shortDescription} We combine business understanding, experience design and engineering discipline to create solutions that can be operated with confidence.</p><div className="flex flex-wrap gap-4 mt-9"><a href="#contact" className="bg-white text-[#0b3c86] rounded-xl px-6 py-3.5 font-bold">Discuss your requirements</a><a href="#capabilities" className="border border-white/40 rounded-xl px-6 py-3.5 font-bold">Explore capabilities</a></div></div></section><nav aria-label="On this page" className="sticky top-[70px] lg:top-[90px] z-30 bg-white/95 backdrop-blur border-b overflow-x-auto"><div className="max-w-[1320px] mx-auto px-6 flex gap-6 min-w-max py-4 text-sm font-semibold text-slate-600">{["Overview","Challenges","Capabilities","Use cases","Process","Technology","FAQ"].map((item) => <a key={item} href={`#${item.toLowerCase().replace(" ", "-")}`} className="hover:text-[#1559bd]">{item}</a>)}</div></nav><section id="overview" className="py-20"><div className="max-w-[1320px] mx-auto px-6 grid lg:grid-cols-[1.1fr_.9fr] gap-14"><div><SectionHeader eyebrow="Service overview" title={`A practical approach to ${service.name.toLowerCase()}.`}/><div className="text-slate-600 text-lg leading-8 mt-7 space-y-5"><p>{service.shortDescription} Businesses often need more than a point solution: they need a clear understanding of the workflow, data, users and operational controls that surround it.</p><p>ARINSA AI MINDS works with stakeholders to define the outcome, assess feasibility and deliver the smallest useful solution that can be measured and improved. The work is shaped around your existing technology landscape rather than a generic implementation pattern.</p><p>Throughout delivery, we keep decisions visible: what the solution does, where information comes from, who can access it, how exceptions are handled and how success will be evaluated after launch.</p></div></div><ArchitectureDiagram service={service}/></div></section><section id="challenges" className="py-20 bg-[#f6f9ff]"><div className="max-w-[1320px] mx-auto px-6"><SectionHeader eyebrow="Business challenges" title="Where this service can help."/><div className="grid md:grid-cols-2 lg:grid-cols-4 gap-5 mt-10">{challenges.map((challenge, index) => <article key={challenge} className="bg-white rounded-2xl border border-slate-200 p-6"><span className="font-bold text-[#1559bd]">0{index + 1}</span><p className="font-semibold leading-7 mt-8">{challenge}</p></article>)}</div></div></section><section id="capabilities" className="py-20"><div className="max-w-[1320px] mx-auto px-6"><SectionHeader eyebrow="Capabilities" title={`What our ${service.name.toLowerCase()} work can include.`} body="The final scope is selected through discovery; each capability is explained, designed and validated in the context of your delivery goals."/><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-5 mt-10">{service.capabilities.map((capability, index) => <article key={capability.title} className="border border-slate-200 rounded-2xl p-6"><span className="text-[#1559bd] font-bold">{String(index + 1).padStart(2, "0")}</span><h3 className="text-xl font-bold mt-6">{capability.title}</h3><p className="text-slate-600 leading-7 mt-3">{capability.description}</p></article>)}</div></div></section><section id="use-cases" className="py-20 bg-[#071B3B] text-white"><div className="max-w-[1320px] mx-auto px-6"><SectionHeader light eyebrow="Common use cases" title="Designed around work people need to get done."/><div className="grid md:grid-cols-3 gap-5 mt-10">{useCases.map(([audience, problem, outcome]) => <article key={audience} className="border border-white/15 rounded-2xl p-6"><p className="text-blue-200 font-bold">{audience}</p><h3 className="font-bold text-xl mt-4">Problem and solution</h3><p className="text-blue-50/85 leading-7 mt-3">{problem}</p><p className="border-t border-white/15 mt-5 pt-5 font-semibold">Expected outcome: {outcome}</p></article>)}</div></div></section><section id="process" className="py-20"><div className="max-w-[1320px] mx-auto px-6 grid lg:grid-cols-2 gap-14"><div><SectionHeader eyebrow="How it works" title="A transparent delivery workflow." body="The detail changes by service, but the engagement stays rooted in practical decision points and continuous validation."/></div><ol className="space-y-5">{[["Discover", "Understand the business objective, users, current process and measures of success."],["Assess", "Review data, systems, risks, constraints and technical feasibility."],["Define and design", "Agree scope, architecture, controls, user flows and delivery priorities."],["Build and validate", "Implement iteratively, test key scenarios and review quality with stakeholders."],["Deploy and improve", "Launch in an appropriate environment, monitor evidence and improve deliberately."]].map(([title, body], index) => <li key={title} className="flex gap-5 border-b pb-5"><span className="text-2xl font-bold text-[#1559bd]">0{index + 1}</span><div><h3 className="font-bold text-xl">{title}</h3><p className="text-slate-600 mt-1">{body}</p></div></li>)}</ol></div></section><section id="technology" className="py-20 bg-[#f6f9ff]"><div className="max-w-[1320px] mx-auto px-6 grid lg:grid-cols-2 gap-14"><div><SectionHeader eyebrow="Technology and controls" title="Technology selected for the problem, not the logo wall." body="Relevant tools may include Python, FastAPI, React, REST APIs, PostgreSQL, ETL pipelines, AI models, vector databases and cloud deployment practices. The selected stack is confirmed during solution design."/></div><div className="bg-white border rounded-2xl p-7"><h3 className="font-bold text-xl">Security and governance considerations</h3><div className="mt-6"><CheckList items={["Role-based access and appropriate data isolation", "Encrypted data transfer and secure API patterns", "Input validation, audit trails and operational monitoring", "Human review and output validation where decisions require oversight", "Clear ownership for exceptions, changes and ongoing maintenance"]}/></div></div></div></section><section><div className="max-w-[1320px] mx-auto px-6 py-20"><SectionHeader eyebrow="Related services" title="Explore connected capabilities."/><div className="grid md:grid-cols-3 gap-5 mt-10">{related.map((item) => <ServiceCard key={item.slug} service={item}/>)}</div></div></section><section id="faq" className="py-20 bg-[#f6f9ff]"><div className="max-w-[900px] mx-auto px-6"><SectionHeader eyebrow="Frequently asked questions" title={`Questions about ${service.name.toLowerCase()}.`}/><FAQ items={genericFaqs(service)}/></div></section><div id="contact"><CTA title={`Discuss your ${service.name.toLowerCase()} requirements.`}/></div></main><Footer/></>; }
